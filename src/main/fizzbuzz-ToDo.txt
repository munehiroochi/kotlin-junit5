#TODO
Fizzbuzz
"fizzbuzz関数は数を文字列に変換する"

  "パラメータライズドテスト"
  パラメーターセットにkey(input, resonなど)を使って書く。値の選定理由が分かりやすい

  "3の倍数のときは数の代わりにFizzに変換する"
    "同値類の最小の3の倍数である3を渡すと文字列Fizzを返す"
    "上限の境界値の一つ内側の値99を渡すと文字列Fizzを返す"

 "5の倍数のときは数の代わりにBuzzに変換する"
    "同値類の最小の5の倍数である5を渡すと文字列Buzzを返す"
    "上限の境界値100を渡すと文字列Buzzを返す"

 "3と5の両方の倍数のときは数の代わりにFizzBuzzに変換する"
　 "15を渡すと数の代わりに文字列FizzBuzzを返す"

  "その他の数のときはそのまま文字列に変換する"
    "下限の境界値1を渡すと文字列1を返す"
    "下限の境界値の一つ内側の値2を渡すと文字列2を返す"

   "仕様の範囲外であっても同じ規則が適用される"
    "下限の境界値の一つ外側の値101を渡すと文字列101を返す"
 　 "下限の境界値の一つ外側の値0を渡すと文字列FizzBuzzを返す"
